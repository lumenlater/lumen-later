# Deployment Configuration
NEXT_PUBLIC_STELLAR_NETWORK=testnet
NEXT_PUBLIC_STELLAR_RPC_URL=https://soroban-testnet.stellar.org
NEXT_PUBLIC_STELLAR_HORIZON_URL=https://horizon-testnet.stellar.org
NEXT_PUBLIC_NETWORK_PASSPHRASE='Test SDF Network ; September 2015'

# Database Configuration
# MongoDB - App data (merchants, bills, etc.)
DATABASE_URL="mongodb://localhost:27017/lumenlater"

# PostgreSQL - Event data (Goldsky Mirror streams here)
# For Supabase: Use pooled connection (port 6543) with pgbouncer=true
# POSTGRES_URL is used for queries (pooled), POSTGRES_URL_DIRECT for migrations (direct)
# Docs: /docs/EVENT_INDEXER.md
#
# Supabase example:
#   POSTGRES_URL="postgresql://postgres.[project]:[password]@aws-0-[region].pooler.supabase.com:6543/postgres?pgbouncer=true"
#   POSTGRES_URL_DIRECT="postgresql://postgres:[password]@db.[project].supabase.co:5432/postgres"
POSTGRES_URL="postgresql://user:password@host:6543/database?pgbouncer=true"
POSTGRES_URL_DIRECT="postgresql://user:password@host:5432/database"

# Goldsky Webhook Secret (for validating webhook requests)
# Generate: openssl rand -hex 32
GOLDSKY_WEBHOOK_SECRET="your-webhook-secret-here"

